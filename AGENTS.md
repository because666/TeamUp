# TeamUp Repository Instructions

本文件是 TeamUp 仓库内所有 AI/Codex 会话的强制规则。关键词 `必须`、`禁止`、`不得` 不可自行弱化。若用户的当前明确指令与本文件冲突，先指出冲突和风险；涉及破坏性操作、数据安全、接口兼容或团队职责变更时必须等待确认。

## 1. 项目与角色

- TeamUp 是面向高校学生竞赛、科研和创业项目的微信小程序组队平台。
- 角色 A 负责产品、前端、AI 模块、运营和赛事材料。
- 角色 B 负责后端、架构、数据库、部署、运维和安全。
- 跨职责修改可以协作，但必须在任务文件中写明修改方、评审方、接口和影响范围。
- 不得把提案、推荐方案或原始 PDF 中的模糊描述表述成已确认决策。

## 2. 每次任务的必读顺序

开始修改前必须依次读取：

1. 本文件。
2. `README.md`。
3. `docs/status/CURRENT.md`。
4. 当前分支对应的 `docs/status/tasks/<TASK-ID>.md`；若任务会改变行为、接口、数据、依赖、部署或产品范围而文件不存在，先按模板创建。
5. 与任务相关的产品、架构、开发、安全和 ADR 文档。
6. 目标目录内更近的 `AGENTS.md`。更近规则只补充其目录约束，不得规避根级安全和协作规则。

不要一次加载全部文档。按 `docs/README.md` 的路由选择当前任务需要的文件。

## 3. 真值与冲突处理

预期行为的优先级：

1. 用户当前明确指令。
2. 状态为 `Accepted` 的 ADR。
3. 已确认的 API、数据模型、PRD 和安全规范。
4. 当前任务文件与 `CURRENT.md`。
5. 原始 PDF 和讨论性材料。

代码和测试代表当前实际实现，但不能静默推翻已确认规范。发现代码、测试和文档不一致时，停止扩大修改，记录差异并请求负责人确定是修代码还是改规范。

文档状态必须使用以下标签之一：

- `Confirmed`：双方已确认，可以实施。
- `Proposed`：有推荐方案，尚待确认。
- `TBD`：信息不足，禁止自行补全为事实。
- `Deprecated`：不再适用，必须指向替代文档。

## 4. 开始任务前

- 运行 `git status --short --branch`，确认分支和未提交改动。
- 运行 `git fetch --prune origin`。若网络不可用，在任务文件记录，不得假装已同步。
- 检查当前分支相对 `origin/main` 的差异。
- 发现他人未提交改动时必须保留；不得 reset、restore、覆盖或顺手格式化这些改动。
- 行为性任务必须使用独立分支，命名见 `docs/development/GIT_WORKFLOW.md`。
- 在任务文件写明目标、范围、非目标、负责人、基线提交和计划修改的接口/文件。
- 需求不清但可用保守且可逆的假设推进时，明确记录假设；会改变产品方向、公共接口、数据模型或安全边界时必须停下确认。

## 5. 实施边界

- 只修改完成当前任务所需的文件，不做无关重构、全仓格式化或依赖升级。
- 优先遵循现有项目结构、框架和工具；工程尚未初始化时不得虚构命令、目录或测试结果。
- 新增生产依赖、切换框架、修改数据库结构、公共 API、认证授权、匹配权重、隐私策略或部署架构前，必须存在 `Accepted` ADR 或用户明确批准。
- API 变更必须同步 `docs/architecture/API_CONTRACT.md`；数据变更必须同步 `DATA_MODEL.md` 和迁移说明。
- 产品行为变更必须同步带编号的 PRD/验收标准。
- AI 输出只能作为辅助信息。不得让大模型直接决定用户权限、信誉处罚或不可解释的录取/淘汰结果。
- 所有外部输入均视为不可信；必须做服务端鉴权、授权、校验和最小化日志处理。
- 不得读取、提交或在输出中暴露密钥、令牌、个人隐私数据、生产连接串和 `.env` 内容。

## 6. 代码与测试

- 前后端共享契约优先于各自临时实现。接口未确认时先更新契约或使用明确标注的 mock。
- 错误必须显式处理，不得吞异常、伪造成功或以占位数据冒充真实数据。
- 注释解释原因和约束，不复述代码。
- 每次行为修改必须增加或更新与风险相称的测试。
- 只报告实际运行过的命令和结果；未运行的检查必须明确写为“未运行”并说明原因。
- 工程命令以 `docs/development/DEVELOPMENT.md` 和项目清单文件为准。二者缺失时先补文档，不猜命令。

## 7. Git 与文件同步

- `main` 仅接收已评审、可集成的改动；两人不得在同一功能分支并行工作。
- 开始和提交前都要同步远程状态。工作区不干净时不得自动 rebase、切换分支或 pull。
- 禁止 `git reset --hard`、覆盖式 checkout、强制推送、无确认删除分支或重写他人提交历史。
- AI 不得自行 commit、push、merge、发布或部署，除非用户在当前任务中明确要求。
- 不得提交 `AGENTS.override.md`。临时覆盖规则不能成为绕过仓库规范的手段。
- 同一项改动的代码、测试、文档和状态文件必须在同一任务分支和同一评审批次中保持一致。
- Pull Request 是可选的评审载体，不是集成前提；无论是否使用 PR，另一角色的评审结论都必须写入 `docs/status/reviews/` 下的评审文件后才能集成到 `main`。

## 8. 状态同步与交接

- 每个行为性任务使用独立文件：`docs/status/tasks/<TASK-ID>.md`。
- 任务文件只由其负责人/分支更新，避免两人同时编辑同一状态文件。
- `docs/status/CURRENT.md` 是 `main` 的聚合快照，只在创建任务、明确交接或合并时更新；不要把逐步日志写进去。
- 需要跨角色交接时，按 `HANDOFF_TEMPLATE.md` 记录已完成内容、接口、验证、未决问题和下一步。
- QQ 只用于通知对方查看分支或文件，不保存项目真值；决定、评审结论、交接和状态必须回写仓库文件。
- 跨角色评审使用 `docs/status/reviews/` 下的独立文件。评审人可只修改该文件，或通过 QQ 发送可原样落盘的完整 Markdown，由任务负责人据实写入。
- 架构或产品决策进入 `docs/decisions/`，不得只存在聊天记录中。
- 临时进展不代替 Git 提交；Git 提交也不代替任务状态和验收证据。

## 9. 完成条件

只有同时满足以下条件，AI 才能宣称任务完成：

- 需求和验收标准逐项满足。
- 相关测试、静态检查或构建已实际执行并记录结果。
- API、数据、产品、安全和运行文档已按影响同步。
- 任务文件状态和验证证据已更新。
- `git diff` 已复查，没有密钥、调试残留、生成垃圾或无关改动。
- 已列出剩余风险、未运行检查和需要另一角色完成的事项。

## 10. Code Review Rules

- 优先检查安全、权限越界、隐私泄漏、数据损坏、并发问题、接口不兼容和需求回归。
- 对每个问题给出文件/行号、触发条件、影响和可执行修复方向。
- 不得仅评论格式问题；可由自动化工具处理的格式问题交给 CI。
- 没有发现问题时也必须说明测试缺口和剩余风险。

## 11. 目录规则

- 修改 `docs/` 时继续遵守 `docs/AGENTS.md`。
- 修改 `.github/` 时继续遵守 `.github/AGENTS.md`。
- 前端和后端代码目录创建后，应各自增加简短的 `AGENTS.md`，仅包含该技术栈的命令、边界和测试要求；不得复制整份根规则。

Codex 的 `AGENTS.md` 发现与覆盖规则参考官方 OpenAI 文档：<https://learn.chatgpt.com/docs/agent-configuration/agents-md>。
