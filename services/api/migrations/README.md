# Database migrations

Status: TBD. The backend foundation intentionally uses an in-memory adapter for local and test runs. MySQL schema, migration tool, rollback procedure, and staging verification must be accepted in a separate data task before production use.

