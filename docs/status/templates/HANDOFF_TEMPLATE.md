# <TASK-ID> 交接记录

> From: 角色 A | 角色 B<br>
> To: 角色 A | 角色 B<br>
> Branch: `<branch>`<br>
> Last Commit: `<sha>`<br>
> Date: YYYY-MM-DD

## 1. 交接原因

说明为何现在交接、接收方要完成什么结果。

## 2. 已完成

- 已实现行为；
- 已确认接口/数据；
- 已更新文档。

## 3. 未完成

- 剩余任务及优先级；
- 不应继续的错误方向。

## 4. 变更入口

| Area | File / API | Notes |
| --- | --- | --- |
| 代码 | `<path>` | `<关键约束>` |
| 契约 | `<link>` | `<版本/状态>` |
| 数据 | `<migration/model>` | `<兼容性>` |

## 5. 验证证据

| Command / Check | Result | Notes |
| --- | --- | --- |
| `<command>` | Passed / Failed / Not run | `<details>` |

## 6. 已知问题与风险

- 触发条件、影响、临时处理和建议修复。

## 7. 接收方第一步

1. 获取并检查指定分支；
2. 阅读任务文件和相关契约；
3. 复现最后验证；
4. 从此处继续：`<specific next action>`。

## 8. 未提交/外部状态

- 是否存在未提交文件：否（交接默认要求）/ 是（列出原因，禁止仅靠本地文件交接）
- 外部配置或人工操作：
- 需要的权限：
